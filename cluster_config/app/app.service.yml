apiVersion: v1
kind: Service
metadata:
  name: k8s-app
  labels:
    name: k8s-app
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80
  selector:
    name: k8s-app