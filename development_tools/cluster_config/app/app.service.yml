apiVersion: v1
kind: Service
metadata:
  name: k8s-test-app
  labels:
    name: k8s-test-app
spec:
  ports:
    - name: symfony
      port: 80
      targetPort: 80
    - name: plain
      port: 8080
      targetPort: 8080
  selector:
    name: k8s-test-app